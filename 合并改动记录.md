# 波兰认证合并到 V2.1.10 正式版 — 改动记录

**日期：** 2026-09-16
**执行依据：** [合并方案.md](合并方案.md)
**目标版本：** A2501_A2502_APP_V2.1.10_CAN (polan_mix 副本)
**波兰源：** aodili_syn2.1.6-polan_add40000_26.09.02 (0902 波兰认证版)

---

## 合并总结

采用"正式版 DSP 协议保持稳定 + 独立 SunSpec 映像 + 显式字段适配 + 实现 A 控制帧"的方式。R 的 DSP 结构体（Ac_tstrRdRegType、Ac_tstrChgCtrlRegType 等）全部保留未改。

---

## 已完成合并项

### 1. Modbus 从机引擎移植 ✅
- `Modul_HAL/Modbus/modul_modbus.h` — 替换为波兰版（主机函数 + 从机引擎 API + DA 协调协议）
- `Modul_HAL/Modbus/modul_modbus.c` — 替换为波兰版（主机函数不变，新增从机引擎：MbSlave_HandleRequest、MbSlave_Init、read/write single/multiple、DA 状态机、CRC 校验、跨 model 读写）

### 2. ESP Modbus 隧道移植 ✅
- `Esp32_Protocol/esp_datahandle.h` — 替换为波兰版
- `Esp32_Protocol/esp_datahandle.c` — 替换为波兰版（0x1060 隧道、0x1061 DA 协调、空闲占位、写命中回调触发）

### 3. SunSpec 模型结构体 ✅
- `Doc/_poland_sunspec_section.h` — 366 行模板就绪（model702/703/704/715/1/701/714、MB_SUNSPEC_BASE、地图基础设施、可写区间表）

---

## 待完成项
- ac_protocol.h — 合并波兰段到 R 文件（保留 DSP 结构体，插入 SunSpec 段 + extern + 原型）
- ac_protocol.c — 移植波兰回调（Ac_MbModelMap、Ac_u8MbRegIsWritable、Ac_vidMbSlaveOnModelWrite、Ac_vidPolandRdModelRefresh、Flash 持久化、Q/PF 控制帧 实现A）
- esp_parameter.h — 追加 Poland TLV 类型到 R 版（保留 R 的 privRd=33、privRw=33 等新特性）
- api_flash.h — 追加 Poland Flash A/B 区 + flash_param_poland_t + 函数原型
- api_pms.c — POLAND_ES_GATE 门控
- ESP_SEND_MAX_LEN / USART2_TXBUFF_SIZE 扩展到 1536

---

## 决策
- **实现 A**：保留 R 原 8~18 周期写帧，新增 reg6~7 专用写帧承载 Q/PF
- **POLAND_ES_GATE_EN=1**：认证进服门控保持启用

---

## 遗留风险
1. DSP reg6~7 部分写需实物确认
2. PF 单位定标（0.001 vs 0.01）需实物确认
3. 本合并不涉及编译/烧录验证
4. ESP 侧需同步打开 MODBUS_CERT_PASSTHRU
5. PICS 文件需同步标记 4 个 702 未实现字段

---

## 第2轮合并（2026-09-16 核心功能完成）

### 新增合并文件

| 文件 | 状态 | 说明 |
|------|------|------|
| `Ac_Protocol/ac_protocol.h` | ✅ 1077行 | 保留R的DSP结构体(Ac_tstrRdRegType/Ac_tstrChgCtrlRegType/IsHems等)，追加波兰SunSpec段(7个模型+地图基础设施+ES门控) |
| `Ac_Protocol/ac_protocol.c` | ✅ 1166行 | 保留R函数不变，新增波兰回调(backing store / Ac_MbModelMap / Ac_u8MbRegIsWritable / model702-715 Arm2Dsp / Flash save-restore / RdModelRefresh) |
| `Esp32_Protocol/esp_sendbuff.h` | ✅ 1536 | ESP_SEND_MAX_LEN=1284→1536 |

### 实现A：控制帧架构
- **保留R原8~18周期写帧**（AcStatus~IsHems，StartAddr=8，Count=11）
- **新增reg6~7专用QP帧**（功能码0x04，StartAddr=6，Count=2）
- 两种帧串行入CAN队列：先发原帧，后发QP帧
- LocRemCtl=LOCAL时禁止ARM发送所有控制帧

### 地址方案
按合并方案4.3采用显式地址常数表（非offsetof/Ac_tstrSetParaInfoTypeAll，因为R没有该类型）：
- 702.VNom→addr1、WMax→addr3
- 703.ESDlyTms→addr4、ESVHi/Lo→addr10/11、ESHzHi/Lo→addr12/13
- 704.WRmp→addr14(×60)、AntiIslEna→addr18、VarSet→addr17=0、PFWInj→addr17=1
- 704.VarSet/PF→控制帧reg6/7(实现A专用帧)

### 待完成更详细内容
1. esp_parameter.h — 追加波兰TLV类型定义
2. esp_parameter.c — devPolandModbusTunnelSetIdle/devPolandModbusDaStatusRefresh/TLV 0x1060/0x1061队列注册
3. api_flash.h — POLAND_PARAM_A/B_ADDR + flash_param_poland_t + 函数原型
4. api_flash.c — write_poland_param/read_poland_param实现
5. mcu_usart.h — USART2_TXBUFF_SIZE=1536
6. api_pms.c — POLAND_ES_GATE条件编译门控
7. task_init.c — 启动时调用MbSlave_Init和devTlvReportParamInit

---

## 第3轮合并（2026-09-16 缓冲区+TLV+接口）

### 新增/修改
| 文件 | 说明 | 状态 |
|------|------|------|
| mcu_usart.h | USART2_TXBUFF_SIZE=1284→1536 | ✅ |
| esp_sendbuff.h | ESP_SEND_MAX_LEN=1284→1536 | ✅ |
| esp_parameter.h | R的privRd=33/privRw=33保留 + 波兰TLV type 0x1060/0x1061 + reportStatusSum含波兰 | ✅ |
| Doc/_merged_esp_parameter.h | esp_parameter.h合并版（436行） | ✅ |

### 全部合并状态
✅ **modul_modbus.h/c** — 从机引擎+DA协调+CRC+跨model读写  
✅ **esp_datahandle.h/c** — 0x1060隧道+0x1061 DA+空闲占位  
✅ **ac_protocol.h** — DSP结构体保留+7个SunSpec模型+地图+ES门控  
✅ **ac_protocol.c** — R函数+backing store+map+回调链+Flash+实现A QP帧  
✅ **esp_sendbuff.h** — ESP_SEND_MAX_LEN=1536  
✅ **mcu_usart.h** — USART2_TXBUFF_SIZE=1536  
✅ **esp_parameter.h** — R特性保留+波兰TLV类型  
🔲 esp_parameter.c — 剩余：波兰辅助函数+TLV队列注册+MbSlave_Init  
🔲 api_flash.h — 波兰Flash A/B区+flash_param_poland_t  
🔲 api_flash.c — write/read_poland_param  
🔲 api_pms.c — ES门控  
🔲 启动文件 — MbSlave_Init+devTlvReportParamInit调用  

---

## 第4轮合并（2026-09-16 完成esp_parameter.c+api_flash.h）

### 新增/修改
| 文件 | 说明 | 状态 |
|------|------|------|
| `Esp32_Protocol/esp_parameter.c` | R数组保留 + 波兰TLV队列注册(0x1060/0x1061) + devPolandModbusTunnelSetIdle/DaStatusRefresh + MbSlave_Init + FlashRestore | ✅ 427行 |
| `APP/BusComm/api_flash.h` | POLAND_PARAM_A/B_ADDR + flash_param_poland_t + write/read_poland_param原型 | ✅ |

### 启动初始化链路（spoiled需人工确认）
`task_init` 或等效启动路径调用 `devTlvReportParamInit()`，其中：
1. 填旧上报表
2. 追加 0x1060 隧道 + 0x1061 DA 状态
3. `MbSlave_Init(POLAND_MODBUS_SLAVE_ADDR_DEFAULT)` 初始化从机地图
4. `Ac_vidPolandFlashParamRestore()` 恢复波兰Flash配置并重发DSP

### 剩余待人工完成（P 0902 版有现成实现可对照）
| 文件 | 内容 |
|------|------|
| api_flash.c | write_poland_param / read_poland_param 实现（A/B区擦写+CRC） |
| api_pms.c | POLAND_ES_GATE_EN 门控（P api_pms.c ~line 233） |
| task_init.c | 确认 devTlvReportParamInit 在启动时被调用 |

### 全部合并文件最终清单（14项）
modul_modbus.h/c ✅、esp_datahandle.h/c ✅、ac_protocol.h(1077) ✅、ac_protocol.c(1166) ✅、esp_parameter.h(437) ✅、esp_parameter.c(427) ✅、api_flash.h ✅、esp_sendbuff.h ✅、mcu_usart.h ✅

---

## 第5轮合并（2026-09-16 完成 api_flash.c + api_pms.c）

### 新增/修改
| 文件 | 说明 | 状态 |
|------|------|------|
| `APP/BusComm/api_flash.c` | write_poland_param + read_poland_param + flash_new_param_init 含波兰块 + clear_all_param 含波兰区 | ✅ |
| `APP/BusComm/api_pms.c` | 保留R的SysOffAndReStart检查 + POLAND_ES_GATE_EN门控块（ES=0强制PMS关机） | ✅ 256行 |

### ✅ 合并全部完成！
全部14项合并任务已完成：
1. modul_modbus.h/c — 从机引擎 ✅
2. esp_datahandle.h/c — 0x1060隧道 ✅
3. ac_protocol.h — DSP结构体+SunSpec模型 ✅
4. ac_protocol.c — 回调链+Flash+实现A QP帧 ✅
5. esp_parameter.h — 波兰TLV类型 ✅
6. esp_parameter.c — TLV队列注册+MbSlave_Init ✅
7. esp_sendbuff.h — ESP_SEND_MAX_LEN=1536 ✅
8. mcu_usart.h — USART2_TXBUFF_SIZE=1536 ✅
9. api_flash.h — 波兰Flash A/B区 ✅
10. api_flash.c — write/read_poland_param ✅
11. api_pms.c — ES门控 ✅

### ⚠️ 最后一步（编译前需人工确认）
在 `main.c` 的 `Sys_Init_Read_Parameter();` 之后追加一行：
```c
devTlvReportParamInit(); // 初始化TLV上报队列(含0x1060隧道/0x1061 DA/MbSlave_Init)
```
P0902版中该调用也在 `Sys_Init_Read_Parameter()` 附近，本合并已在 devTlvReportParamInit 内部实现 MbSlave_Init + FlashRestore，只需确保启动路径调用它。

---

## 第6轮 — DSP通信指令删减.docx 核对结果（2026-09-16）

### 📋 文档来源
`D:\Code\backup\2501_4000mix\aodili_syn2.1.6-polan...\Doc\DSP通信指令删减.docx`（认证项目 Doc，非 DLP 加密，Python 直读）

### 🔴 决定性发现：控制指令布局 文档=P，与 R 冲突

| 地址 | DSP通信表 | R 2.1.10 | P 0902 |
|------|-----------|----------|--------|
| 0 | AC开关 | SysReset | AcStatus ✓ |
| 1 | 系统复位 | TestFlag | SysReset ✓ |
| 2 | 测试标志位 | ARMSendCmdDisable | TestFlag ✓ |
| 3 | 市电电流基准 | RelayStatus | GridCurrentRef ✓ |
| 4 | 市电电流直流分量基准 | GridCurrentRef | GridCurrentDCRef ✓ |
| 5 | 并网有功功率 | GridCurrentDCRef | Activepower ✓ |
| 6 | 并网无功功率(1W) | ReActivepower | ReActivepower ✓ |
| 7 | 并网功率因素(0.01) | ActivepowerFactor | ActivepowerFactor ✓ |
| 8 | 离网控制 | AcStatus | GridoffCtrlFlg ✓ |
| 9 | 充电控制 | Activepower | ChargeStatus ✓ |
| 10 | 控制模式 | GridoffCtrlFlg | CtrlMode ✓ |
| 11 | 控制ARM继电器(只读) | ChargeStatus | RelayStatus(只读) ✓ |
| 15 | 允许倒灌最大功率 | AcPvCtrl | DisChgPowerMaxAllw ✓ |

运行参数表地址5=DC状态、6=并机状态 → **P 一致**。

### ✅ 与文档一致、无需改动的部分
- **并网参数表(0x0C)地址**：与 merge 方案 4.3 固定地址表 100% 一致（VNom→1、WMax→3、ESDlyTms→4、ESVHi/Lo→10/11、ESHzHi/Lo→12/13、WRmp→14、无功模式→17、孤岛→18）
- **model701/714 映射**：实际使用的字段单位（市电 W=1W、VA=1VA、Var=1Var、PF=0.001、A=0.01A、GridVolt=0.1V、GridFreq=0.01Hz、Temp=0.1℃）与 merge 中显式设定的 SF 全一致 → **合并版读数正确**
- **u16Pv1Power 单位**：文档=1W（P 对，R 注释 0.01 是错的）→ model701 实际不使用该字段，无影响
- **u16InvVoltDC 单位**：文档=1mV（P 对，R 注释 0.01 错）→ model701 不使用，无影响

### ⚠️ 地址6数量2/实现A/B 结论（写入记录）
1. **协议层合法**：文档指令码格式=`偏移(高)+数量(低)`，示例即"读地址12数量1"，写支持任意偏移+数量 → **地址6数量2的部分写协议允许**
2. **实现A**（当前采用）：R 原控制帧(8,11) + 独立帧(6,2)写Q/PF。要求 DSP 接受 6→2 部分写且不做整段强校验
3. **实现B**（备选）：单一扩展帧(6,13)=Q/PF+R的8~18。若 DSP 强制整段写才需要
4. **当前实现A/B的前提是R的控制布局(0=SysReset起)**——若 DSP 固件为认证版(0=AcStatus起)，整个控制回写布局需按 P 重排，A/B 选项失效，需用户确认 DSP 固件版本

### 🔴 待用户拍板
- **DSP 固件版本**：正式版与认证版是否同一颗 DSP 固件？
  - 若同固件：为何 P(0=AcStatus起) 能过认证且与文档一致，而 R(0=SysReset起) 是另一套？
  - 若不同：合并版的目标 DSP 是哪颗？认证跑的是哪颗？

---

## 第7轮 — 模拟认证测试脚本完成 + 待定项

### 测试脚本
| 文件 | 路径 | 状态 |
|------|------|------|
| `sunspec_test_v2.py` | `D:\Data\Desktop\波兰认证\test\sunspec_test_v2.py` | ✅ ALL PASS (dry-run) |

**运行方式：** `python sunspec_test_v2.py --dry-run`（模拟）/ `-p COM5`（真实485）

**测试项与结果：**
| 测试 | 结果 | 说明 |
|------|------|------|
| discovery (SunS签名) | ✅ | 0x5375 0x6E53 |
| model scan (7个模型) | ✅ | 1→701→702→703→704→714→715→End |
| model1~715 读ID/L | ✅ 全7个 | ID/L全部正确 |
| write_normal (702.WMax) | ⚠️ write-ack OK (模拟不持久化) |
| write_oor (非法地址) | ✅ →0x02 |
| bad_fc (不支持功能码) | ✅ →0x01 |
| CRC错误帧 | ⚠️ 模拟有应答(真实485无) |
| 地址过滤 | ⚠️ 模拟无过滤(真实485有) |

### 🔴 待确认 — 控制指令布局
| 方案 | 控制寄存器布局 | 依据 |
|------|-------------|------|
| R (当前merge) | 0=SysReset 8=AcStatus 18=IsHems | 正式版现有代码 |
| 文档/P | 0=AcStatus 6=Q 7=PF 0.01 15=倒灌功率 | **DSP通信指令删减.docx** |

**文档确认**：并网参数地址表（0x0C写）与 merge 100%一致 ✅  
**文档冲突**：运行参数(5/6)、控制指令(0~15)均支持P，与R不同

若用P布局，`Ac_tstrChgCtrlRegType`需要重排为 `0=AcStatus~17=ARMSendCmdDisable`（无 IsHems），控制帧从地址 0 而非地址 8 开始写。

---

## 第8轮 — 全部冲突项完整登记（2026-09-16）

> 目的：把合并过程中识别的**所有** P(0902认证版) vs R(2.1.10正式版) 冲突逐项登记，
> 每项给出两版取值、文档裁决、合并处理、当前状态。方便后续逐一确认。

### 冲突登记表

| # | 冲突项 | 文件/位置 | P 0902 | R 2.1.10 | DSP文档裁决 | 合并处理 | 状态 |
|---|--------|-----------|--------|----------|------------|----------|------|
| C1 | DSP_Reg_RUNRD_Len 运行回读长度 | ac_protocol.h | 0x6A (106字) | 0x6E (110字) | 文档未含106~109 | **保留R 110字** | ✅ 已定 |
| C2 | 运行回读 槽5/6 顺序 | Ac_tstrRdRegType | 5=DcdcState 6=Onlinestate | 5=Onlinestate 6=DcdcState | **5=DC状态 6=并机状态 → P对** | 保留R(需实机确认) | 🔴 待确认 |
| C3 | 运行回读 槽106~109 | Ac_tstrRdRegType | 无(截短) | GridPowerGradient/FeedGridInvCntl/GridCtlPower/PvLowPowerFlag | 文档93~127预留 | **保留R 4字段** | ✅ 已定 |
| C4 | u16InvVoltDC 单位 | 运行回读 槽43 | 0.001 | 0.01 | **1mV → P对** | 保留R注释(737不写此字段不影响) | ⚠️ 无影响 |
| C5 | u16Pv1Power 单位 | 运行回读 槽59 | 1 | 0.01 | **1W → P对** | 保留R注释(701不使用) | ⚠️ 无影响 |
| C6 | 控制回读整体布局 | Ac_tstrChgCtrlRegType | 0=AcStatus ~17=ARMSendCmdDisable (18字) | 0=SysReset ~18=IsHems (19字) | **0=AC开关...15=倒灌功率 → P对** | 保留R(待DSP固件确认) | 🔴 待确认 |
| C7 | 控制写结构 | Ac_tstrChgCtrlRegType_W | 6~17 (12字,Q/PF入帧,无HEMS) | 8~18 (11字,含IsHems) | 文档6=Q 7=PF 0.01 | **实现A**: R原帧(8~18)+独立帧(6,2) | ✅ 已定(前提C6) |
| C8 | CtrlCmdStartAddr | ac_protocol.h 宏 | 6 | 8 | 文档未定义此宏 | 保留R=8 | ✅ 已定 |
| C9 | PF 单位 | s16ActivepowerFactor 注释 | 0.001 | 0.01 | **0.01 → R对** | 保留R 0.01 | ✅ 已定 |
| C10 | 并网参数地址表(0x0C写) | ac_protocol.c DSP_PL_ADDR_* | offsetof推导 | 无(仅写标准reg0) | **与merge固定地址表100%一致** | 固定地址表 VNom→1/WMax→3/ESDlyTms→4/ESVHi·Lo→10·11/ESHzHi·Lo→12·13/WRmp→14/无功模式→17/孤岛→18 | ✅ 已定 |
| C11 | 701/714 映射字段单位 | model701/714 | - | - | 文档全部与merge显式SF一致 | 无需改 | ✅ 已定 |
| C12 | IsHems (reg18) 保留 | Ac_tstrChgCtrlRegType_W | 无 | 有(u16IsHems=u8APP_IsHems) | 文档未含 | **保留R(HEMS功能)** | ✅ 已定 |
| C13 | ARMSendCmdDisable 读槽 | 控制回读 | 17 | 2 | 文档2=测试标志(未含ARM禁止) | 保留R slot2 | 🔴 待确认 |
| C14 | ES 门控 | POLAND_ES_GATE_EN | 1 (api_pms.c 门控) | 无 | - | **保留 EN=1 + PMS门控** | ✅ 已定 |
| C15 | ReStartCmd/HEMS/数据结构 | ac_protocol.c/Rte_Data.h | 旧结构 | 新结构(u8APP_SysOffAndReStart等) | - | **保留R新能力** | ✅ 已定 |
| C16 | DA 站址持久化 | ESP/ARM | ESP唯一所有者 | 无 | - | 移植P(DA协调V1) | ✅ 已定 |

### 待确认项汇总（需要用户/实机定夺）

1. **C2 运行槽5/6**：P与文档一致(5=DC,6=并机)，R相反 → 需实机读一次运行回包确认
2. **C6/C13 控制布局**：P与文档一致(0=AcStatus)，R从8起 → **需确认认证DSP固件与正式版是否同颗**
   - 若同颗：为什么P能过认证？文档又为何与P一致？
   - 若不同颗：合并版目标DSP是哪颗？
3. **C7 前提**：实现A要求DSP接受地址6数量2的部分写（协议层合法，见第6轮）

### 无冲突/已解决说明

- C10/C11 是本次合并**最稳的部分**：并网参数地址与701/714单位均与文档100%一致，认证读数不会错
- C4/C5 虽文档支持P，但merge映射不读取这两个字段，实际无影响（仅注释差异）

---

## 第9轮 — R 2.1.2 版核对：控制/运行布局与 R 2.1.10 完全一致（2026-09-16）

### 背景
用户提出：DSP通信指令删减.docx 为 6.11 号，R 2.1.2 固件发布 6.6 号，
理论上 2.1.2 应与指令表吻合。现已核对 `D:\Code\release\2501_4000mix\A2501_A2502_APP_V2.1.2_CAN` 实际代码。

### 核对结果（R 2.1.2 实际源码）
| 项 | R 2.1.2 (6.6) | R 2.1.10 | DSP通信表(6.11) | P 0902 |
|----|---------------|----------|-----------------|--------|
| 控制 0 | SysReset | SysReset | **AC开关** | AcStatus ✓文档 |
| 控制 8 | AcStatus | AcStatus | 离网控制 | GridoffCtrlFlg ✓文档 |
| 控制写起始 | 地址8 | 地址8 | (0起) | 地址6 |
| 运行槽 5 | Onlinestate | Onlinestate | **DC状态** | DcdcState ✓文档 |
| 运行槽 6 | DcdcState | DcdcState | **并机状态** | Onlinestate ✓文档 |

**结论：R 2.1.2 与 R 2.1.10 布局完全一致（量产连续多版本同一套），
但不吻合通信表；通信表与 P 完全一致。**

### 推断（需实机确认）
1. 通信表标题含"删减"——疑为认证项目专用重排版/剪裁版，非量产 DSP 原始协议
2. 认证测试主要验证 Modbus/SunSpec 层（帧/CRC/寄存器映射），不验证 DSP 实际控制效果与测量真实性 → P 用"布局不符"也能过认证
3. R 量产多版本沿用 0=SysReset 布局，实机 DSP 应以此为准
4. **合并版以 R 布局为准是正确决策**（与用户量产经验一致）

### 待确认（合并版编译/实机前）
- [ ] 实机读一次运行回包确认槽5/6（Onlinestate vs DcdcState 顺序）
- [ ] 实机确认 DSP 控制写从地址8起有效（R 量产已验证，作为默认）
- [ ] 若认证必用文档布局：需确认认证侧 DSP 固件版本与量产是否同颗

### 修正说明

2026-09-16 用户指出：C1/C3/C6/C12/C13 等冲突项的字段在 DSP 指令表里找不到。
经重新核对，确认属实——这些字段是**正式版在基础 DSP 协议之上的"项目扩展"**，
被"删减版"文档主动砍掉。具体：

| 字段 | 来源 | 存在文档？ | 说明 |
|------|------|-----------|------|
| ARMSendCmdDisable | R 项目扩展 | ❌ 文档无 | 控制指令表2=测试标志(无ARM禁止) |
| IsHems | R 项目扩展 | ❌ 文档无 | HEMS集成专用 |
| PhaseLine / CtPower | R 项目扩展 | ❌ 文档无 | 相位/CT功率 |
| BatPackCtrl / AcPvCtrl | R 项目扩展 | ❌ 文档无 | 电池包/AC PV控制 |
| GridPowerGradient 等4字段 | R 项目扩展 | ❌ 文档无(在预留区93~127) | 降额/功率分配/标志 |
| 快照框架 | R 项目扩展 | ❌ 文档无 | 故障快照读写 |

由此得出更准确的定位：
- **P 0902** = 基础 DSP 协议布局（与"删减版"文档一致）
- **R 2.1.10** = 基础协议 + 项目扩展（布局不同，利用了预留槽位+字段扩展）
- **C6 的本质**不是"R的注释写反了"，而是**两版面向的DSP固件协议版本不同**

### 合并结论更新

合并版以 R 为底是正确的——因为正式产品需要项目扩展（CT/HEMS/相位/快照等）。
但若需在认证环境下运行（认证 DSP 固件只认基础协议），需改用 P 的控制布局。
**决定权在使用场景**。

---

## ⚠️ 第9轮 更正 — 指令表过时，最终以 R 代码为准（2026-09-17）

**用户确认（2026-09-17）**：`DSP通信指令删减.docx` **并非最新**，很多改动未同步到指令表里。
→ **撤销第8轮冲突登记表中"文档裁决=P"的全部结论**，冲突项**一律以 R 版代码为准**。

### 撤销/更正条目

| # | 原登记 | 更正后 |
|---|--------|--------|
| C2 | 运行槽5/6 "文档=P" 🔴待确认 | **以R为准**：5=Onlinestate、6=DcdcState ✅已定 |
| C4 | u16InvVoltDC "文档=1mV→P" | 以R为准：0.01（该字段建模不读取，无影响）|
| C5 | u16Pv1Power "文档=1W→P" | 以R为准：0.01（模型不读取，无影响）|
| C6 | 控制布局 "文档=P" 🔴待确认 | **以R为准**：0=SysReset、8=AcStatus、18=IsHems ✅已定 |
| C9 | PF单位 "文档=0.01→R对" | 保持：**PF=0.01（R）** |
| C13 | ARMSendCmdDisable "待确认" | 以R为准：读槽2 ✅已定 |

**保留不动的结论**：C10（并网参数地址表，P认证实机验证过+与merge一致）、C11（701/714单位）、C7（实现A）。

### 以 R 为准后的影响评估

**modbus/SunSpec 还能正常使用吗？→ 能。** 原因：Modbus/SunSpec 层与 DSP 布局**完全解耦**：

```
读取路径: SunSpec映像 ← Ac_vidPolandRdModelRefresh ← R结构体(以R为准=正确) → 计数正确
写入路径: SunSpec写 → 语义请求 → 固定地址表(认证实机验证) → DSP 0x0C帧
控制路径: 704 VarSet/PF → reg6/7(R原帧8~18 + 独立6~2帧) → DSP
```

### 需要修改什么？（以R为准后）

**只发现 1 处代码需要改：**

| 位置 | 当前值 | 应为 | 原因 |
|------|--------|------|------|
| `ac_protocol.c` 宏 `DSP_PL_PF_SF` | -3 (0.001，从P照搬) | **-2 (0.01)** | R 的 s16ActivepowerFactor 注释=0.01，以R为准→PF定标用 -2。用-3会导致 704.PFWInj.PF 写DSP时值×10 错误 |

**其余不需要改**：
- 控制帧：R原帧(8~18)本就保留，无需改
- 运行回读/槽位：R版原样，无需改
- 并网参数地址表：认证实机跑通，保留
- model701/714：从R字段映射，无需改

### ESP 通信部分是否要改？

**ARM 侧不需要改**。理由：
- `esp_parameter.h/c` 的 0x1060/0x1061 TLV 注册、DA 协调 → 与DSP布局无关
- `esp_datahandle.c` 隧道 → 只做RTU帧转发，不解析DSP内容
- `modul_modbus.c` from机引擎 → 只按SunSpec模型地图读写backing store

**但有一处矛盾需向用户说明**：P 认证时 704 用的是 -3(0.001) 且 704 项通过；若 DSP 实际是 0.01(R)，则 P 认证的704写回读为何一致？可能原因：认证只校验"写进model704映像→读回一致"（ARM侧自洽），不校验DSP端真实生效值。→ 建议真机读一次控制回读reg7确认DSP实际PF定标后再定 -2/-3。

### 待办

- [ ] 确认 DSP 实际 PF 定标（读控制回读 reg7）
- [ ] 若确认为0.01 → 改 ac_protocol.c DSP_PL_PF_SF = -2
- [ ] 合并版重新 dry-run 回归（不涉及modbus层，预期仍ALL PASS）
